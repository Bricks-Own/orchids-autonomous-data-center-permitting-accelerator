Comprehensive Construction Platform fix: full spreadsheet coverage audit, realistic Day-1 data, heading cleanup, and real dynamism
Three separate problems to fix in ConstructionDashboard.jsx / server/constructionData.js, plus a full audit pass against the original spreadsheet. Don't touch the core EVM/threshold formulas (calcEVM, getTrafficLight) — those are correct. Everything else in this prompt is fair game.

1. Full field-by-field audit against the spreadsheet — find and fill any gaps
Go through every field below and confirm it's actually rendered somewhere in the dashboard with a real value pulled from data/evm/safety/quality/schedule/cont/chg — not just a section that exists in spirit. Report back which fields, if any, are missing entirely.

C-Suite Dashboard: Total Development Cost (TDC), Baseline Approved Budget vs Forecasted EAC, Milestone Volatility (Variance Days), Contingency Burn Rate vs Physical Progress, Unapproved Claims & Change Order Exposure, TRIR, Top 5 Risk Register (RED/YELLOW only).

Leadership Reporting — confirmed gap: the table currently has 5 traffic-light columns (CPI, SPI, Schedule, Safety, Quality), but the spreadsheet asks for exactly these 5: Safety Performance, CPI, SPI, Progress (Planned vs Actual), and Cash Flow. "Quality" isn't requested and Cash Flow is missing entirely. Fix: replace the "Quality" column with a "Cash Flow" traffic-light column (derive from data.netCashPosition — green if positive, red if negative), and rename "Schedule" to "Progress" if it represents planned-vs-actual progress. Confirm Project_ID, Project_Name, Date_As_Of_Date, Revenue, Planned/Forecast Margin are all present.

HSE: Total_Hours, Safe_Days_Since_Incident, First_Aid_Incidents, Recordable_Incidents_TRIR, Lost_Time_Incidents_LTIR, Safety_Observations_Resolved.

Quality: Open_RFI_Count, Avg_RFI_Response_Time_Days, Open_Punchlist_Items, Rework_Cost_Incurred.

Financials: Original Project Cost, Actual_Cost_To_Date, Earned_Value_To_Date, EAC, VAC, Pending Change Order Count/Value, Original/Remaining Contingency, Contingency Burn Rate, Billed_To_Date, Cash_Received_To_Date, Days for Receivable, Net Cash Position, CPI, VAC.

Schedule/Progress/Milestone: Customer's Need Date, Baseline/Forecast Finish Date, SPI, Milestone Variance Days, per-milestone name/variance/percent-complete/critical-path-impact.

Other Construction Trackers: GC Buyout %, Owner vs GC Contingency, Stored Materials Value Offsite, PCO Age Days, Weather Days Claimed, RFI Critical-Path Impact Count, AHJ Permit Status, Cx Prerequisites %, Lien Waiver Compliance, AHJ Inspection Pass Rate, Actual vs Planned Daily Headcount, Retainage Withheld, Net Payable This Month.

Cost and Schedule Category (Vendor Ledger): Category dropdown, Vendor Name, Original/Current Commitment, Approved/Pending Change Orders, Invoiced/Paid to Date, AP Aging, EAC — per vendor row.

2. Fix Day-1 data realism — a freshly generated site should not show fake mid-project progress
Confirmed root cause: generateConstructionData() computes constructionStart by working backward from codTarget minus an industry-standard duration estimate, then computes progress from how far today's real calendar date falls inside that backward-derived window. A site generated for the first time right now can immediately show "77% physical progress" and "73% contingency burned" — numbers never actually entered or tracked.

Fix: a freshly generated site (never had saveConstructionData called on it) should show Day-1 EVM state: percentComplete: 0, plannedPctComplete: 0, ACWP: 0, EV: 0, EAC: BAC, contingency used $0, physical progress 0%. Only after a real "Update Metrics" save should numbers move. Check whether a construction_metrics DB row already exists for the site — only the first-ever generation should default to Day-1 zero-state; existing saved data should still load normally.

3. Remove arbitrary heading colors — confirmed bug
Four CardTitle elements on the C-Suite Dashboard tab have hardcoded colors with no consistent logic:

<CardTitle className="text-amber-400">Milestone Volatility</CardTitle>
<CardTitle className="text-blue-400">Contingency Burn Rate vs Physical Progress</CardTitle>
<CardTitle className="text-destructive">Unapproved Claims & Change Order Exposure</CardTitle>
<CardTitle className="text-primary">Safety & Health Compliance — TRIR</CardTitle>
Remove the color class from all four to match the neutral default every other CardTitle on this page uses. Sweep the file for any other CardTitle with a hardcoded text-* color and fix those too.

4. Make it more dynamic — real cross-navigation, not static tabs sitting next to each other
Doesn't need full BI cross-filtering, but nothing currently reacts to anything else. Add real, working connections:

Top 5 Risk Register items should link to the relevant detail tab (a PSD-timeline risk → Schedule tab; a GC labor risk → Other Trackers/Vendor Ledger), using the same setActiveTab pattern already used elsewhere in the app.
Confirm every KPI card that should have a FormulaPopover actually has one — check for gaps.
Active Alerts banner should be clickable and jump to the relevant tab, not just sit there as static text.
Don't fabricate interactivity that doesn't do anything real — every link added must navigate somewhere genuinely relevant.

What NOT to change
Don't touch calcEVM(), getTrafficLight(), or core threshold/formula logic.
Don't touch AdvancedCharts.jsx, VendorLedger.jsx, BaselineProjections.jsx, FormulaPopover.jsx, SiteView3D.jsx internals.
Don't remove currently-working metric coverage — additive/corrective only.
Verify after
Confirm every spreadsheet field is findable with a real value; confirm Leadership Reporting has all 5 correct traffic lights (Safety, CPI, SPI, Progress, Cash Flow).
Generate a brand-new never-saved site — confirm Physical Progress/Contingency Used/Actual Cost all start at 0. Then use "Update Metrics" and confirm numbers reflect the real entry.
Confirm all four C-Suite card titles are neutral-colored, and no other stray CardTitle colors exist.
Click a Risk Register item and the Active Alerts banner — confirm both navigate to genuinely relevant tabs.