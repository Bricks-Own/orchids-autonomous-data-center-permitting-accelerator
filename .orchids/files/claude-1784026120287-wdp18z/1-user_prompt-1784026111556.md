CORRECTION + FINAL POLISH PASS — a previous prompt from me incorrectly instructed adding rounded corners (rounded-2xl on cards, rounded-md on buttons/inputs, rounded-full on badges/stepper) to match a hand-written spec that predates using the actual shadcn preset. That was wrong. The b1zM2TW91E preset ("base-sera" style) is intentionally sharp/square-cornered — no border-radius on cards, buttons, inputs, or badges by default. Revert every manual rounded-* class that was added on top of the preset's native components in the last two rounds of fixes. Do not apply any custom radius override anywhere — let the shadcn primitives (Button, Card, Input, Select, Badge, etc.) render exactly as the CLI scaffolded them originally, with zero manual styling layered on top of what the preset provides.

GROUND RULE FOR THIS ENTIRE PASS: do not invent, approximate, or hand-adjust ANY visual property — spacing, radius, color, shadow, font size, line height — that the shadcn theme/preset already defines. If a component needs a certain look, install/use the real registry component and its real variant props. Never write custom Tailwind classes that override or approximate what the preset's design tokens already specify. This app's UI should be indistinguishable in quality and detail from a stock shadcn dashboard built with this exact preset — not "inspired by," not "close to," exactly using it.

ISSUES TO FIX:

1. Corners: remove every manually-added rounded-* class from AuthModal, Header, SiteIntake, and Stepper. Let the preset's native sharp-cornered style stand as-is on every Card, Button, Input, Select, Badge, Dialog. Verify by inspecting the actual computed border-radius on a Card and a Button — it should be 0 (or whatever the untouched primitive naturally computes to), not a manually-added value.

2. Navigation: replace the current horizontal sliding tab bar (Header.jsx) entirely. Install and use shadcn's actual Sidebar component (npx shadcn@latest add sidebar — check what's available in this preset/registry) — a left-hand sidebar with icon + label navigation items, grouped sections, matching the preset's native sidebar styling exactly (no custom colors/spacing layered on). This app has 13 top-level sections (Platform Overview, Executive Summary, Site Planner, Site Intake, 4 permit-type tabs, Milestone Timeline, Document Factory, Digital Twin, Compliance OS, Regulator Copilot, Knowledge Hub, Construction Platform) — a sidebar is the correct pattern for this many sections, not a horizontal strip. Preserve the existing permit-type-based tab pruning logic (hiding Air/Water/Building/Power nav items based on inputs.permitTypesNeeded) in the new sidebar.

3. Spacing / overflow: audit every screen in this migration (login, sidebar nav, all 3 Site Intake wizard steps) for text overflow, cramped padding, or inconsistent gaps. Use the preset's own spacing scale (whatever spacing tokens/utilities ship with the theme) consistently — don't hand-pick padding values per component. No text should ever clip, wrap awkwardly, or sit flush against a card edge.

4. Full re-check against the preset, not just these 4 files: go through src/index.css and the components.json config again, confirm every token (spacing, radius, font sizes, line-heights, shadows) is being consumed via the theme rather than hardcoded, and confirm nothing else from the earlier "revamp" and "fixes" commits introduced custom overrides that fight the preset. Fix anything found.

CONSTRAINTS: no data logic, calculation, or field-wiring changes — presentation layer only. Don't touch server/routes.js's './lib/...' import paths. Preserve all existing functionality: permit-type + project-type selection, dynamic form sections, the Traditional-vs-Brick timeline comparison on the Review step, and (in the new sidebar) the same nav-pruning behavior Header.jsx has today.

VERIFY: screenshot the login screen and all 3 Site Intake wizard steps with the new sidebar nav. Confirm zero manually-added border-radius anywhere (inspect computed styles on at least a Card and a Button to prove it). Confirm no text overflow at 1280px and ~1024px widths. Zero console errors. Build succeeds. 41/41 backend tests pass.