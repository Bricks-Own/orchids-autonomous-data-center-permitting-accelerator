UI ACCURACY PASS — theme tokens are confirmed correct (verified directly against the official preset export at https://ui.shadcn.com/create?preset=b1zM2TW91E&template=vite&pointer=true&rtl=true — our src/index.css color variables are byte-identical to the preset's). The remaining gap is entirely in how components are composed: spacing, font sizing, icon sizing, and structural patterns don't match the reference. Fix by copying real reference code, not by approximating further.

STEP 1 — Get the real reference source, don't guess from description.
In a scratch/throwaway directory (NOT inside this project), run:
  pnpm dlx shadcn@latest init --preset b1zM2TW91E --template vite --pointer --rtl
This scaffolds the exact dashboard shown at the URL above as real, runnable component files (sidebar, stat cards, layout, dialogs, charts). Open that generated project and use its ACTUAL component source code as the literal reference for structure, spacing, and sizing — copy real patterns from real files, do not re-approximate from memory or prose description. Also browse https://ui.shadcn.com/create?preset=b1zM2TW91E&template=vite&pointer=true&rtl=true directly and inspect it (computed styles, DOM structure) for anything not obvious from the source alone.

STEP 2 — Specific fixes, using that reference project as ground truth for each:

- Font sizes are too small throughout Site Intake and the sidebar. Compare directly against the reference's actual type scale (check its real computed font-size/line-height on headings, card titles, body text, labels) and match it exactly — do not invent a scale.
- Icon sizes are too small. Check the reference's actual Phosphor icon size prop values in sidebar nav items, card headers, and buttons, and match exactly.
- Container padding on cards (Permit Types Needed, Project Type, Air/Water/Building/Power Parameters sections, etc.) is tighter than the reference. Match the reference's actual card padding values.
- Sidebar must be properly collapsible, not stuck in an unlabeled icon-only state. Right now it defaults to collapsed with icons that have no visible label, making it unreadable. Default it to EXPANDED (labels + icons visible) with a working, visible toggle button (check how the reference project's actual Sidebar component implements its collapse trigger — it should already have one built in, use it) so the user can collapse to icon-only themselves if they want, but the default state must be understandable without hovering over every icon.
- The "Continue" / "Generate My Permits" button placement at the bottom of each wizard step is cramped against the page edge with no breathing room. Check how the reference project handles its own primary action button placement/spacing (footer bar pattern, card padding, etc.) and match that.
- Selects, dropdowns, and any chart/stat displays in Site Intake don't match the reference's actual Select/Chart component implementation. Use the real shadcn Select component and (if the reference uses one) its chart component pattern exactly as scaffolded in the reference project — don't hand-build approximations.

CONSTRAINTS: this is layout/spacing/sizing only — no data logic, calculation, or field-wiring changes. Don't touch server/routes.js's './lib/...' import paths. Preserve all existing functionality: permit-type + project-type selection, dynamic form sections, the Traditional-vs-Brick timeline comparison on Review, and the sidebar's nav-pruning logic (hiding Air/Water/Building/Power items based on inputs.permitTypesNeeded).

VERIFY: side-by-side screenshot comparison — Site Intake (all 3 steps) and the sidebar against the actual reference project running locally. Font sizes, icon sizes, card padding, and button placement should be visibly matching, not just "in the same ballpark." Sidebar opens expanded and readable by default with a working collapse toggle. Zero console errors. Build succeeds. 41/41 backend tests pass.