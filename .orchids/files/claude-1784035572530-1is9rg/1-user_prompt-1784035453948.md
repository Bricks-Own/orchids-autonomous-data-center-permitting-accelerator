CRITICAL FUNCTIONAL BUGS — multiple interactive elements on Site Intake don't work at all, not just cosmetic issues. Debug each with actual interaction testing (open the dropdown, try dragging the slider) and computed-style inspection, not just visual review of the JSX.

BUG 1 — State select opens but shows a completely black/empty dropdown, no visible options.
Repro: Site Intake step 2, click the "State" field. The dropdown opens but renders as large solid-black boxes with zero visible text — not missing some items, completely blank. Meanwhile the "Turbine / Engine Type" select on the SAME page (also using the same Select/SelectContent/SelectItem components from src/components/ui/select.jsx) renders correctly with visible option text. The only structural difference is list length: State has 50 items (US_STATES), Turbine Type has 3.
Investigate: (a) whether this is another invisible-text CSS bug like the earlier double-wrapped oklch() issue — inspect computed color/background-color on a SelectItem inside the State dropdown specifically, confirm text color isn't matching/blending into the background; (b) whether the long 50-item list triggers a height/overflow/virtualization problem in SelectContent's max-h-(--available-height) sizing that Turbine Type's 3-item list never exercises — check what happens with a medium-length list (10-20 items) to narrow down if it's list-length-dependent; (c) whether SelectPrimitive.Positioner's alignItemWithTrigger positioning breaks with more items and pushes content outside the visible popup bounds while the backdrop still renders. Fix whatever the actual cause is, then verify by actually opening the State dropdown and selecting a real state, confirming the value updates and dependent logic (attainment lookup, address autofill) still fires.

BUG 2 — Brick Load Reduction slider does not respond to dragging at all.
Repro: Site Intake step 2, "Brick Dispatch Optimization" card. The Slider (src/components/ui/slider.jsx, @base-ui/react/slider-based, wired in SiteIntake.jsx with value={[inputs.brickSavings || 0]} / onValueChange={([v]) => update('brickSavings', v)}) renders visually correctly (track, fill, thumb all styled) but clicking/dragging the thumb produces no response — the value never changes. Debug the actual pointer/drag event handling: check for an overlapping element with a higher z-index blocking pointer events on the thumb, a missing required prop/wrapper the @base-ui/react Slider needs to register drag handlers, or an onValueChange signature mismatch (confirm whether this Slider's callback actually passes an array vs a single number — verify against the installed library's actual API, don't assume). Verify by actually dragging the thumb (or clicking at a specific point on the track) and confirming inputs.brickSavings updates and the "X% reduction" label reflects it live.

BUG 3 — Dropdown option text gets cut off / truncated illegibly.
Repro: the Turbine/Engine Type dropdown shows "Gas Turbine (standard combustion" and "Gas Turbine (older frame, uncontro..." — both clipped mid-word, illegible. The dropdown/select popup width needs to either grow to fit the longest option's content (min-width based on content, not a fixed narrow width) or wrap text onto a second line instead of hard-truncating. Audit every Select in Site Intake for the same issue, not just this one instance.

BUG 4 — Collapsed sidebar icon rail clips/chops icons.
Repro: collapse the sidebar to icon-only mode — the icons in the narrow rail appear cut off at the edges rather than centered with proper padding within the collapsed width. Check the collapsed-state width/padding on the Sidebar and SidebarMenuButton components (group-data-[collapsible=icon]: variants) and ensure icons render fully visible and centered, not clipped by the container.

For all four: this is about things ACTUALLY WORKING when clicked/dragged, not just looking plausible in a screenshot. Test each interaction yourself (open every select and pick a non-default option, drag the slider to a new value, collapse the sidebar and inspect icon alignment) before considering any of these fixed.

CONSTRAINTS: no data logic or field-wiring changes beyond what's needed to fix the actual interaction bugs. Don't touch server/routes.js's './lib/...' import paths.

VERIFY: for each bug, describe the exact interaction you performed and its result (e.g. "opened State select, selected California, confirmed inputs.state updated and Air Quality Area Status changed to reflect it"). Zero console errors. Build succeeds. 41/41 backend tests pass.