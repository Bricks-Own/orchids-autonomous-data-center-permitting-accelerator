UI REVAMP — Site Intake gets a permit-type selection modal + full visual cleanup. This applies on top of the permit-type-driven redesign already in place (permitTypesNeeded, dynamic form sections, Header nav pruning — don't touch that logic, this is a visual/UX layer on top of it).

DESIGN DIRECTION: sleek, dark, professional — think shadcn/ui's aesthetic: neutral zinc/slate surfaces, subtle 1px borders, generous whitespace, restrained color used only as small accents (icons, selection rings) — NOT as background washes or gradients. No gradient buttons, no glowing shadows, no "AI generated" look. Every card should feel intentional and consistent, not ad hoc.

ICONS: Install @phosphor-icons/react. Use the "duotone" weight, thin/clean style, for every icon in this redesign — permit-type icons, section headers, buttons. Suggested icons: Air = Wind, Water = Drop, Building = Buildings, Power = Lightning, Site Planner = MapTrifold, selection check = Check.

PART 1 — Permit Selection Modal (new component, e.g. src/components/PermitSelectionModal.jsx)

Use @headlessui/react's Dialog (already installed) for accessibility (focus trap, Escape, overlay). Build:
- A centered dialog, max-w-xl, rounded-2xl, border border-zinc-800, bg-zinc-950, shadow-2xl. Backdrop: black/70 with backdrop-blur.
- Header: "What are you working on?" + one line of explanatory copy: "Select the permits you're pursuing — the intake form will only show fields that are relevant. You can change this anytime."
- A 2x2 grid of selectable permit-type cards (Air / Water / Building / Power), each: duotone icon top-left, label, one-line description, and a small checkmark badge that appears top-right when selected. Selected state = subtle ring in the permit's accent color (violet/sky/indigo/amber) + slightly lighter background — NOT a filled color background. Unselected = neutral border, hover state lightens slightly. Multi-select (matches the existing permitTypesNeeded array).
- Below the grid, a distinct full-width row: "Not sure yet? Plan your site first" with a MapTrifold icon (emerald accent) — clicking this closes the modal and navigates directly to Site Planner (setActiveTab('siteplanner')) WITHOUT setting any permit type selection.
- Footer: selected count on the left, "Continue" button on the right (disabled/greyed until at least one permit type is selected). Primary button style: solid light (bg-zinc-100 text-zinc-900, hover:bg-white) — a neutral high-contrast button, not a colored/gradient one.
- Dismissable via Escape/backdrop-click ONLY if the user has previously confirmed a selection (i.e., they're revisiting/editing) — on first-ever visit for a site with no permitTypesNeeded set yet, the modal cannot be dismissed without choosing at least one option or picking Site Planner.

WIRING: In src/components/SiteIntake.jsx, show this modal automatically on mount when inputs.permitTypesNeeded is not yet set (first time this site's intake has been configured). Also add a small persistent affordance on the page itself — e.g. a compact summary row showing the currently selected permit type icons/labels with a "Change" button — that reopens the modal at any time (dismissable this time, since a selection already exists). On confirm, set inputs.permitTypesNeeded and close the modal; the existing dynamic-section logic already reacts to that value, don't change it.

PART 2 — Site Intake visual cleanup (same file)

Replace the current inconsistent styling (each section has a different colored background wash — violet-950/10, blue-950/10, indigo-950/10, yellow-950/10 — which reads cluttered) with ONE consistent neutral card style used everywhere: rounded-2xl border border-zinc-800 bg-zinc-900/50 p-5. Differentiate sections only via a small duotone icon + accent-colored icon (not a background tint) next to each section heading — Site Information, Generation Overview, Data Center Systems, Air/Water/Building/Power Parameters.

Fix these existing bugs while you're in this file:
- Several places use raw \u escape sequences directly as JSX text content, which JSX does NOT interpret outside of a string/expression — they currently render as literal backslash-u codes on screen instead of the intended character. Specifically: "Phase 1 \u2192 instant" should render as "Phase 1 → instant", "(vs. 2\u20133 weeks manual)" should render as "(vs. 2–3 weeks manual)", "Auto-derive \u26A1" should render as "Auto-derive ⚡", the permit checklist's "\u2713" checkmarks, "\u26A1 Generate My Permits", "\u2713 Screening Complete", and the "\u2192 {label}" results-nav buttons. Fix all of these — either use real unicode characters directly, or wrap them in a JS expression like {'\u2192'} so the escape is actually interpreted.
- No overflowing text: check the "Automated acceleration" stat box in the header, long dropdown labels (State, Fire Suppression, Occupancy Type), and the results-nav button grid at small viewport widths — everything must wrap or truncate cleanly, no horizontal overflow or clipped text.
- Restyle the primary "Generate My Permits" button and the results-complete panel to match the new neutral/duotone-icon aesthetic instead of the current indigo-gradient-ish button.
- Restyle Field/Input/Select base components (used throughout this file) to a cleaner look: rounded-lg border border-zinc-800 bg-zinc-950 text-zinc-100, focus:border-zinc-600 focus:ring-1 focus:ring-zinc-600/50, labels text-zinc-500.
- Keep every existing data key, onChange handler, and calculation wiring EXACTLY as-is — this is a presentation-layer pass only, not a data model change.

CONSTRAINTS:
- Don't touch calculations.js, documentGenerator.js, buildPowerCalc.js, or any of the permit-type filtering logic already built into Header.jsx/App.jsx.
- Don't modify the dynamic import paths in server/routes.js ('./lib/...') — every past PR has reverted these, please leave them alone this time.
- This redesign should be scoped to Site Intake and its new modal only for this pass — do not touch other tabs/pages yet, more UI work will follow in later prompts.

VERIFY: fresh site (no permitTypesNeeded) → landing on Site Intake immediately shows the modal, cannot be dismissed without a selection or picking Site Planner; selecting Air + Power → Continue → modal closes, form shows only Universal + Air + Power sections, matching the existing filter logic; the compact "Change" affordance reopens the modal and this time IS dismissable; picking "Site Planner" from the modal navigates there directly; zero raw \u escape codes visible anywhere on the page; no horizontal overflow at 1280px and at a narrow ~768px width; zero console errors; 41/41 backend tests still pass; build succeeds.