APP-WIDE DESIGN SYSTEM MIGRATION — apply an official shadcn theme preset across the ENTIRE application: every screen, every component, with no exceptions — including the login/register screen, which is easy to forget since it's outside the main app shell.

STEP 0 — Confirm access first.
Run: npx shadcn@latest apply --preset b1zM2TW91E
If this fails to reach the registry (network/DNS error, 403, timeout) — STOP and report that back before doing anything else, rather than silently falling back to hand-written CSS that only approximates the preset. If it succeeds, continue.

STEP 1 — Install the theme + pull in shadcn's actual components.
The apply command scaffolds components.json, the cn() utility, and Tailwind theme CSS variables (--background, --foreground, --primary, --secondary, --muted, --border, --input, --ring, --radius, etc.). On top of that, explicitly pull in shadcn's real registry components via the CLI (npx shadcn@latest add <component>) rather than hand-building lookalikes — at minimum: button, card, input, select, dialog, badge, tabs, table, tooltip, popover, slider, dropdown-menu, form, label, separator, avatar (for user account display). Use these actual shadcn components throughout the app, not custom divs styled to resemble them — that's the point of using shadcn: consistent, accessible, maintained primitives, not a one-off imitation.

Since this app has no light/dark toggle today — it's always dark — apply the preset's dark theme values as the single active theme.

STEP 2 — Migrate EVERY screen, with zero exceptions. This explicitly includes:
- src/components/AuthModal.jsx — the login/register screen. This is the very first thing every user sees and is NOT optional or lower-priority just because it's outside the main authenticated app shell — it must use the same shadcn Dialog/Card/Input/Button/Form components and theme tokens as everywhere else, not be left on the old styling.
- src/components/Header.jsx — the main nav bar (use shadcn Tabs or an equivalent, preserve the existing permit-type tab-pruning logic).
- Every tab: Overview, ExecutiveSummary, SitePlanner, SiteIntake + PermitSelectionModal (preserve all wizard/Project-Type/Review/Brick-vs-traditional-timeline functionality from the flow-rebuild prompt — re-skin it onto shadcn components, don't rebuild the logic), AirPermitAI, WaterPermitAI, BuildingPermitAI, PowerPermitAI, MilestoneTimeline, Document Factory, DigitalTwin, ComplianceOS, RegulatorCopilot, KnowledgeHub (incl. its article modal), ConstructionDashboard and every sub-component (VendorLedger, BaselineProjections, AdvancedCharts, FormulaPopover, SiteView3D's side panel), the floating SiteAssistant chat widget.
- Every modal/dialog in the app (permit selection, article viewer, login/register, any confirmation dialogs) → shadcn Dialog, consistently.
- Every data table (Vendor Ledger etc.) → shadcn Table.
- Every tooltip/formula-explainer popover → shadcn Tooltip/Popover.

CONSTRAINTS:
- Do NOT change any data logic, calculation functions, API calls, auth flow, state management, or field names anywhere — this is a component/visual migration only. Login must still authenticate exactly as before, every onClick/onChange/inputs.xyz key must keep working.
- Do NOT touch the dynamic import paths in server/routes.js ('./lib/...').
- Do NOT lose any functionality built in the flow-rebuild prompt (permit modal, Project Type selection, Review screen, Traditional-vs-Brick timeline metric, nav pruning) — carry all of it forward on the new components.
- Icons: @phosphor-icons/react duotone only, used sparingly, no emojis.
- Brand accent: sparing use only (logo + one or two active-state highlights), everything else neutral per the preset's tokens — no gradients, no glow/colored shadows.
- Work systematically: theme + shared components first, then AuthModal (since it's the entry point), then nav shell, then tab-by-tab. Confirm no breakage after each major section.

VERIFY: grep the whole src/components/ directory for any leftover old hardcoded classes (bg-gray-900, border-gray-700, bg-indigo-*, etc.) — none should remain outside of files intentionally left alone (none should be left alone in this pass). Login/register flow works end-to-end on the new theme. Every tab loads with zero console errors and visually matches the new theme. The Site Intake wizard/Review/timeline feature still works correctly. 41/41 backend tests pass (backend untouched). Build succeeds with no dependency conflicts.