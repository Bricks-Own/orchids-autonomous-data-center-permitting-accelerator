Good, all confirmed. Let's fix Critical + High priority now, one at a time — verify each fix works before moving to the next one. Don't remove functionality, replace fake/static logic with real logic tied to actual site inputs.

1. SSRF risk — server/routes.js:529-568
/agent/web-fetch accepts any arbitrary url from the request body and fetches it directly with no validation. Restrict it to a fixed allowlist of trusted regulatory domains only (epa.gov, ecfr.gov, ghgreporting.epa.gov, netdmr.epa.gov, and relevant state agency domains). Reject any URL outside the allowlist with a clear error.

2. Compliance OS hardcoded "compliant" — src/components/ComplianceOS.jsx:11-144
All 12 CONDITIONS have hardcoded compliance: 'compliant' status. Replace with real logic that derives status from actual calculated data — e.g. compare current NOx/CO/water values against the permit cap, compare gensetHours against the enforceable hour limit, compare runtime against the synthetic minor threshold. Status should be computed as "compliant," "near limit," or "exceeded" based on real comparison, not static.

3. Random document match score — src/components/ComplianceValidationPanel.jsx:442
ragMatchScore: 92 + Math.floor(Math.random() * 8) is a random number, not real analysis. Replace with a real rule-based completeness check — verify the document actually contains the required regulation citations and correct site-specific values for that document type, and compute a score based on how many required elements are actually present and correct.

4. Chatbots silently fall back with no warning — RegulatorCopilot.jsx:125-146, SiteAssistant.jsx:179-188, server/llm.js:248-251
When the AI is unavailable, all three surfaces quietly return a weaker canned/RAG-only answer with no clear warning to the user. Make the frontend reliably surface the existing fallback note from llm.js, and add a visible banner/badge (e.g. "Limited mode — AI not connected") wherever a fallback response is shown, instead of it blending in as a normal answer.

5. All signups get admin role — server/auth.js:33
Registration hardcodes role: 'admin' for every new user. Add proper role-based access control — new signups should default to a standard user role. Admin should be assignable separately, not automatic on registration.

6. Building size formula too small — src/components/SiteIntake.jsx:36 and :144
Currently uses 125 sqft/MW, producing unrealistically small buildings (16,625 sqft for a 133MW site). Update to a realistic industry ratio — roughly 750-1,000+ sqft/MW depending on density — and apply it consistently in both places this formula appears.

Go through these in order, confirm each fix individually with the actual updated behavior (not just "done"), then move to the next. Once these 6 are complete, I'll send the Medium priority batch (items 4, 7, 9, 11, 12, 14, 16).