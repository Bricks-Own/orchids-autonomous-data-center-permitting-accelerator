 rebuild Milestone Timeline around real data, fix Site Intake persistence/sizing/layout

Part 1 — Milestone Timeline shows generic industry boilerplate, completely disconnected from the actual site
Right now permitOS/src/components/MilestoneTimeline.jsx shows three blocks of pure static reference content that have nothing to do with the site's actual calculated timeline:

The "key metrics" cards hardcode '9–18 months', '12–24 months', '6–12 months', '2–4 weeks' — generic industry ranges, not this site's real numbers.
MILESTONE_PHASES (in permitOS/src/data/permitData.js, line 230) is a fully static 6-phase array with fixed, overlapping week ranges ('1–2', '2–4', '4–8'...) — unrelated to any calculation.
The Gantt chart's getGanttTracks() computes its own separate, duplicated pathway math instead of using the shared computePathwayDuration()/computeTimelineComparison() from timelineCalc.js — so it doesn't reflect the Brick-acceleration fixes from earlier sessions at all.
This is why the Milestone page showed totally unrelated numbers from the Site Intake Review page for the same site (Review said "9 months traditional / 7 months Brick-accelerated" — Milestone page showed generic "9–18 months" boilerplate with no connection to that 9/7 figure at all).

What this page should actually be (matches the user's intent — "isn't milestone what work will be done at different phases"): a real breakdown of the site's actual computed timeline into phases, shown twice — once for Traditional, once for Brick-Accelerated — so the user can see exactly what's happening week-by-week in both scenarios and how much time Brick saves at each phase, not just the headline total.

Fix:

1a. Add a shared phase-breakdown utility to permitOS/src/utils/timelineCalc.js (single source of truth, used by both Site Intake and Milestone Timeline):

const PHASE_WEIGHTS = [
  { label: 'Site Intake & Screening', weight: 0.08 },
  { label: 'Applicability Screening & PTE', weight: 0.10 },
  { label: 'Technical Analysis & Modeling', weight: 0.20 },
  { label: 'Document Generation & Assembly', weight: 0.15 },
  { label: 'Agency Submission & Review', weight: 0.35 },
  { label: 'Permit Issuance', weight: 0.12 },
];

export function getPhaseBreakdown(totalWeeks) {
  let cursor = 0;
  return PHASE_WEIGHTS.map(p => {
    const start = Math.round(cursor) + 1;
    cursor += totalWeeks * p.weight;
    const end = Math.round(cursor);
    return { label: p.label, startWeek: start, endWeek: Math.max(end, start), weeks: Math.max(end - start + 1, 1) };
  });
}
1b. In SiteIntake.jsx's renderStep3(), replace the hardcoded timelinePhases array with getPhaseBreakdown(comparison.brickAccel.totalWeeks) — the Phases Overview chart already built there should show the real Brick-accelerated breakdown, not fixed generic ranges. Keep using the same ChartContainer/BarChart component already in place, just feed it real data.

1c. Rewrite MilestoneTimeline.jsx:

Delete the static "key metrics" boilerplate cards entirely.
Replace MILESTONE_PHASES-driven phase cards with two phase-breakdown timelines side by side (or stacked on smaller screens) — one built from getPhaseBreakdown(comparison.traditional.totalWeeks) labeled "Traditional Pathway (X months)", one from getPhaseBreakdown(comparison.brickAccel.totalWeeks) labeled "Brick-Accelerated (Y months)" — using the same real shadcn chart component pattern from Part 1b (reuse it, don't reinvent). You'll need to call computeTimelineComparison(inputs, results) here the same way SiteIntake.jsx does.
Fix the Gantt chart's duplicate label bug: GanttBar renders {label} baked into the colored bar and the row renders track.label again as a separate leading column — every row shows its own name twice. Remove the {label} text from inside GanttBar (the row's leading label is enough; the bar itself should just be a colored range, no redundant text).
Update getGanttTracks() to derive its Air track durations from the same computePathwayDuration() used in timelineCalc.js instead of its own separate duplicated formula, so this chart's numbers actually agree with the rest of the app.
The "Additional Permit Requirements" table (OTHER_PERMITS) is also fully static, unrelated to real site data — flag this to the user before removing, this wasn't explicitly called out but is the same category of issue as everything else here.
Verify: on a site where Traditional computes to 9 months and Brick-Accelerated to 7 months (per the Review step), Milestone Timeline should show two breakdowns whose phases actually sum to 9 and 7 months respectively — not generic ranges. Change the site's inputs and confirm both breakdowns update accordingly.

Part 2 — Site Intake should reopen at Step 3 once permits exist
In SiteIntake.jsx, const [step, setStep] = useState(1) always starts at Step 1 regardless of whether permits were already generated. Change to initialize based on done (results !== null):

const [step, setStep] = useState(() => (results !== null ? 3 : 1));
Verify: generate permits, navigate away to another tab, come back to Site Intake — should land directly on Step 3 (Review & Generate), not Step 1.

Part 3 — Generate button is too large
The "Generate My Permits" button uses the default Button size at flex-1 width, which reads as oversized next to the Reset button. Change to size="sm" and drop the flex-1 stretch (or keep both buttons a fixed, modest width instead of stretching to fill the row).

Part 4 — Grid layout: cards should pack into open slots, not drop to a lonely full-width row
The permit-type breakdown grid in Step 3 (grid grid-cols-1 md:grid-cols-2 gap-5, wrapping the conditional Air/Water/Building/Power cards) and the new phase-breakdown cards from Part 1c should use dense grid packing so that when there's an odd number of items, a leftover card fills an open slot in an earlier row rather than awkwardly spanning full width or sitting alone with unused space next to it. Add grid-flow-dense (or equivalent) and test with 1, 2, 3, and 4 derived permit types to confirm sensible packing at each count — full width should only happen when there's truly no room anywhere else, not by default.

Part 5 — Use real shadcn components everywhere, not just Site Intake
Apply the same standard: everywhere in the app that currently hand-rolls a chart/timeline/progress visualization with raw divs and inline styles (Milestone Timeline being the main offender here), replace it with the real shadcn ui/chart.jsx component already pulled into this codebase, consistent with how the Site Intake Review step's Phases Overview chart was done. Same theme colors (--chart-1 through --chart-5), same component patterns.

Verify everything end-to-end before calling this done: run a full site intake, confirm Site Intake Review, the new Milestone Timeline breakdowns, and the Gantt chart all show numbers that agree with each other and with the actual computed pathway for that specific site — not three disconnected sets of numbers like before. 41/41 backend tests should still pass, build clean.