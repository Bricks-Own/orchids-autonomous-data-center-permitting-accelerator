FULL COMPONENT MIGRATION + INTERACTION QA — this is the fourth pass on this UI and the recurring root cause every time is the same: components get installed from the registry but never actually swapped in, so hand-rolled/native HTML elements keep surviving in the app. This pass must eliminate that pattern completely, not patch another instance of it.
STEP 1 — Install the COMPLETE component set, not a selection.
Go to https://ui.shadcn.com/create?preset=b1zM2TW91E&template=vite&pointer=true&rtl=true, open "Get Code" → "Existing Project" → "Full preset", and install every single component the registry offers for this preset (not just the ~15 already added) — including but not limited to: slider, switch, radio-group, textarea, tabs, accordion, alert, alert-dialog, breadcrumb, calendar, command, context-menu, hover-card, menubar, navigation-menu, pagination, progress, scroll-area, toggle, toggle-group, chart. Even ones not obviously used yet — install the full set now so future passes never have to "add one more component."
STEP 2 — Full sweep: find and replace EVERY remaining native/hand-rolled form element.
Grep the entire src/components/ directory for: <input type="range" (found: SiteIntake.jsx line ~367, the "Brick Load Reduction" slider still uses a raw range input with a custom .brick-slider CSS class instead of the real Slider component that's already installed at src/components/ui/slider.jsx — this exact "installed but unused" bug pattern must not exist anywhere else either), plain <select> elements, plain <input> elements not already wrapped as the shadcn Input component, custom checkbox/toggle markup, and any inline arbitrary-pixel Tailwind classes (text-[11px], text-[13px], etc.) that survived outside of SiteIntake.jsx (which was already fixed to use the proper text-xs/text-sm/text-base/text-lg scale — use that file as the internal reference baseline for the correct scale, and apply the same fix to every other file: Header.jsx, App.jsx's sidebar, AuthModal.jsx, and any other file touched by this redesign). Replace every one found with the real shadcn component.
STEP 3 — Fix specific reported bugs:
(a) Sidebar cannot be reopened once collapsed. In src/App.jsx, the SidebarTrigger is wrapped in a div with className="group-data-[collapsible=icon]:hidden" — meaning when the sidebar collapses to icon-only, the ONLY control to expand it again is also hidden, trapping the user in the collapsed state. The trigger must remain visible and clickable in BOTH expanded and collapsed states — move it outside that hidden wrapper, or give it its own always-visible placement (e.g., pinned in the collapsed icon rail, or in the top Header bar regardless of sidebar state).
(b) State selector (and other Selects) in Site Intake are reportedly broken/not working. Actually click it, open the dropdown, scroll it, select a state, and confirm the value updates and the dependent logic (attainment status lookup, address auto-fill) fires — don't assume the JSX wiring is correct just because it compiles. Check for: dropdown rendering off-screen or behind other elements (z-index/overflow-clipping inside the Card), the 50-state list being unstyled/unusably dense, or any interaction failure. Fix whatever is actually broken and confirm by performing the full click-open-select-close interaction yourself.
(c) Sidebar and nav icons are still too small, and font size across the rest of the app (outside Site Intake, which is already fixed) is still small. Sweep every file this redesign has touched and bring icon sizes and type scale up to match the corrected SiteIntake.jsx baseline and the actual reference project's real sizing — nav icons especially should be clearly legible at a glance, not squinting-small.
STEP 4 — Mandatory interactive QA before calling this done (not just "it builds"):
Actually click through every single interactive element you touched: open the sidebar, collapse it, confirm you can reopen it. Open every Select/dropdown in all 3 Site Intake wizard steps and confirm each one opens, scrolls, and selects correctly. Drag the Brick Load Reduction slider (once it's the real Slider component) and confirm the value updates. Click through all 3 wizard steps end to end. Test on both a fresh site (defaults) and after changing several fields. If something doesn't visibly work when you click it, it's not done — this is the standard for this pass, not "the code compiles."
CONSTRAINTS: no data logic, calculation, or field-wiring changes — presentation/interaction layer only, though fixing broken interaction wiring (like the Select) counts as this layer, not a data-logic change. Don't touch server/routes.js's './lib/...' import paths. Preserve all existing functionality: permit-type + project-type selection, dynamic form sections, the Traditional-vs-Brick timeline comparison, sidebar nav-pruning logic.
VERIFY: screenshot the sidebar in both expanded and collapsed states with a working reopen control visible in both. Screenshot all 3 Site Intake steps showing the corrected type scale and icon sizes. Demonstrate (describe or screenshot) the State select actually opening and a selection being made. Demonstrate the Brick Load Reduction slider as a real shadcn Slider component, not a raw range input. Zero console errors. Build succeeds. 41/41 backend tests pass.