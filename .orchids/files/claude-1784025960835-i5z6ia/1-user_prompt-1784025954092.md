UI QUALITY PASS — the shadcn migration is structurally working now (real components imported, theme colors rendering correctly after the last fix) but the visual execution is far below the preset's actual quality bar: spacing, padding, typography hierarchy, and card density all look off. This needs a real polish pass, not just "it builds."

STEP 1 — Re-run the official init with the exact flags below (this may re-scaffold some pieces more correctly than the earlier `apply` command did):
  yarn dlx shadcn@latest init --preset b1zM2TW91E --template vite --rtl --pointer
Resolve any prompts/conflicts it raises with the existing setup (don't blindly overwrite working app logic — merge intelligently, keep all existing functionality).

STEP 2 — Match this quality bar exactly (this is what the b1zM2TW91E preset looks like when done right, use these as concrete targets, not vibes):
- Generous card padding (~24-28px interior padding, not cramped 12-16px).
- Consistent grid gutters between cards (~16-24px gap, uniform both directions).
- Stat/metric card pattern: small uppercase muted-gray label on top, then a LARGE bold numeric value (28-32px) directly below it, minimal decoration — this hierarchy (label small+muted, value large+bold) should be used everywhere a number/metric is displayed, not just buried in body text.
- Progress bars: thin rounded track in a muted color, filled portion in the primary accent color with rounded ends, a small label/percentage to the side.
- Section headers: bold title + one small action button/pill top-right (not stacked, not centered) — e.g. "NEW GOAL", "VIEW ALL", "VIEW FULL REPORT" pattern.
- List rows (transactions, activity, etc. — apply this pattern anywhere we show a list): small icon-in-rounded-square on the left, label + sub-label stacked in the middle, value/meta right-aligned, subtle hover state, thin separator between rows not full card borders.
- Consistent card corner radius (12-16px) and consistent thin 1px border color across every card, everywhere.
- Typography: consistent type scale across the whole app — establish and reuse a real scale (e.g. 11px labels/eyebrows, 13-14px body, 15-16px section titles, 28-32px large stat values), consistent line-height (~1.5 for body text, tighter ~1.2 for large numbers), consistent font-weight usage (400 body, 500-600 labels/emphasis, 700-800 large numbers/headings) — no ad hoc sizing per component.

STEP 3 — Reconsider the top navigation. The current horizontal sliding tab bar (Header.jsx) does not match the density/quality of a real shadcn dashboard and won't scale cleanly with this many sections (Platform Overview, Executive Summary, Site Planner, Site Intake, 4 permit-type tabs, Milestone Timeline, Document Factory, Digital Twin, Compliance OS, Regulator Copilot, Knowledge Hub, Construction Platform). Evaluate switching to a left sidebar navigation instead — shadcn has an official Sidebar component/block in its registry (npx shadcn@latest add sidebar, or check what the b1zM2TW91E preset provides) with icon + label list items, section grouping, and collapse/expand — this is the standard pattern for apps with this many top-level sections and will look far more like a real product than a horizontal tab strip. If you determine a sidebar genuinely doesn't fit this app's flow, keep the top nav but bring it to the same spacing/padding/typography quality bar as everything else — don't leave it as the current cramped strip either way.

STEP 4 — Use ONLY real shadcn registry components, do not hand-build approximations. For every pattern above (stat cards, progress bars, list rows, section headers, sidebar/nav), check what's already available in src/components/ui/ or the shadcn registry (Card, Progress, Badge, Avatar, Separator, Table, Sidebar, etc.) and use those directly with their intended props/variants. If a needed primitive isn't installed yet (e.g. Progress, Sidebar), install it via the CLI (npx shadcn@latest add <component>) rather than writing a custom lookalike — that's exactly how the last two rounds of bugs happened (missing radius, broken theme tokens) and it must stop happening.

SCOPE: apply this quality pass to every page this migration has touched so far — AuthModal (login), Header/nav, and the full SiteIntake wizard (all 3 steps: Project Setup, Site Details, Review & Generate). Don't expand scope to untouched pages yet.

CONSTRAINTS: do not change any data logic, calculation functions, or field wiring — this is presentation-layer only. Do not touch the dynamic import paths in server/routes.js ('./lib/...'). Preserve all existing functionality (permit-type + project-type selection, dynamic form sections, the Traditional-vs-Brick timeline comparison, nav pruning logic) — re-skin it, don't rebuild the logic.

VERIFY: take a screenshot of the login screen and each Site Intake wizard step, and self-check them against the concrete visual targets in Step 2 line by line (padding, gutter spacing, stat hierarchy, progress bar style, section header pattern, list row pattern, corner radius consistency, type scale consistency) before considering this done. Zero console errors. Build succeeds. 41/41 backend tests pass.