Make Construction Platform look like a real product, gated on real site data, structured like an actual PowerBI report
ConstructionDashboard.jsx (1136 lines) already has strong underlying data and math — real EVM formulas (CPI/SPI/EAC/VAC/TCPI), traffic-light thresholds matching industry standards, and generateConstructionData() on the backend genuinely computes budget/schedule/cost from real Site Intake inputs. Don't touch any of that calculation logic. This is a display-layer and information-architecture pass only.

1. Gate the entire page on real site data — don't show fabricated numbers for a site that was never configured
Right now ConstructionDashboard.jsx calls fetchConstructionData(inputs?.siteName || 'default', inputs, results) unconditionally, and the backend defaults every field to the default BigWatt site — meaning a user who has never run Site Intake still sees a fully populated dashboard, none of it tied to anything they actually did.

Fix: check results from usePermitData() before fetching or rendering. If results is null, show the same empty-state pattern already used in Permits.jsx and MilestoneTimeline.jsx — a centered message with two buttons: "Generate via Site Intake" and "Plan via Site Planner." Only call fetchConstructionData once real results exist.

2. Restructure into two clear tiers instead of one flat list of 14 tabs
The spreadsheet is structured as an executive rollup + operational detail underneath. The current UI flattens that into 14 same-weight tabs.

Restructure as:

Landing view: the C-Suite Dashboard — TDC, Baseline Budget vs Forecasted EAC, Milestone Volatility, Contingency Burn Rate vs Physical Progress, Unapproved Claims & Change Order Exposure, Safety (TRIR) — with the Top 5 Risk Register auto-populated from whichever tracked metrics are currently RED/YELLOW, exactly as the spreadsheet calls for.
Leadership Reporting: dedicated tab, rendered as an actual table (project ID/name, as-of date, revenue, planned/forecast margin, 5 traffic-light columns) using shadcn Table — a portfolio-grid pattern, structurally ready for more projects later.
Operational detail tabs, grouped under shadcn Tabs: HSE, Quality, Financials, Schedule, Cost Categories, Other Trackers, PCO Tracker, Vendor Ledger, Baseline & Projections, Advanced Charts, 3D Site View.
3. Convert the visual layer to the app's real shadcn Sera theme
KpiCard/TrafficDot (hardcoded bg-green-900/20/bg-amber-900/20/bg-red-900/20) → real shadcn Card with Badge, using the semantic convention already used elsewhere (text-primary for on-track, amber for at-risk, text-destructive for critical).
Every emoji icon (🏛️📊🛡️✅💰📅📁📋⚠️📝📈📉🏗️✏️) → Phosphor icons, weight="duotone".
Raw button-based tab switcher → shadcn Tabs/TabsList/TabsTrigger/TabsContent.
Any raw <table> markup → shadcn Table components.
Keep AdvancedCharts.jsx's existing chart components (BudgetWaterfall, SCurveChart, ContingencyDrawdown, MilestoneVarianceChart, CPISPITrend) — real Recharts components already, just match their surrounding chrome to the shadcn Card pattern.

4. Header and empty-state consistency
Match the plain-heading page-header style used elsewhere. "Update Metrics" button → shadcn Button with a Phosphor PencilSimple icon instead of the ✏️ emoji.

What NOT to change
Don't touch generateConstructionData(), calcEVM(), getTrafficLight(), or any threshold/formula logic in server/constructionData.js.
Don't touch internals of AdvancedCharts.jsx, VendorLedger.jsx, BaselineProjections.jsx, FormulaPopover.jsx, SiteView3D.jsx — only outer container styling if needed.
Don't reduce metric coverage — this is a restyle/restructure, not a feature cut.
Verify after
Fresh session, never touch Site Intake — go directly to Construction Platform. Confirm empty state shows, not a populated dashboard.
Run a real Site Intake screening, then open Construction Platform — confirm real, populated data computed from that specific site (change turbine count or state and confirm numbers shift).
Confirm C-Suite Dashboard is the landing view with a working, live-derived Top 5 Risk Register.
Confirm Leadership Reporting renders as a real table with working traffic-light badges.
Click every operational detail tab — real shadcn Tabs navigation, real Card/Badge/Table components, zero emoji left.