real routing, Context Provider, Generate/Reset button, dynamic Milestone filter

This is a bigger architectural pass — please work through it in this order and verify each part before moving to the next.

Part 1 — Generate button should lock after success, add a Reset button
In permitOS/src/components/SiteIntake.jsx, the "Generate My Permits" button (disabled={running}) only disables while the request is in flight, then re-enables regardless of whether results now exists — so it can be clicked repeatedly after success. Fix:

Change the disable condition to disabled={running || done} (done is already defined as results !== null).
Add a "Reset" button next to it, visible only when done is true, that clears results (setResults(null)) and re-enables Generate. Use the existing Button variant="outline" pattern used elsewhere in this file.
Part 2 — Add a Context Provider for inputs/results instead of prop drilling
Right now App.jsx holds inputs/results in local useState and manually passes them as props into every single case of the renderTab() switch. Create permitOS/src/context/PermitDataContext.jsx:

import React, { createContext, useContext, useState } from 'react';
import { defaultInputs } from '../App';

const PermitDataContext = createContext(null);

export function PermitDataProvider({ children }) {
  const [inputs, setInputs] = useState(defaultInputs);
  const [results, setResults] = useState(null);
  return (
    <PermitDataContext.Provider value={{ inputs, setInputs, results, setResults }}>
      {children}
    </PermitDataContext.Provider>
  );
}

export function usePermitData() {
  const ctx = useContext(PermitDataContext);
  if (!ctx) throw new Error('usePermitData must be used within PermitDataProvider');
  return ctx;
}
(Move defaultInputs into this file instead if that's cleaner — either way, keep it exported from wherever it ends up so nothing else that imports it breaks.)

Wrap <App /> in PermitDataProvider in main.jsx. In App.jsx, replace the local inputs/results state with const { inputs, setInputs, results, setResults } = usePermitData();. Then remove inputs={inputs} and results={results} from every line in renderTab()'s switch and instead have each page component call usePermitData() itself internally to get what it needs. Update every component currently receiving inputs/results as props (SiteIntake, Permits, AirPermitAI, WaterPermitAI, BuildingPermitAI, PowerPermitAI, MilestoneTimeline, DocumentFactory, DigitalTwin, ComplianceOS, RegulatorCopilot, ExecutiveSummary, KnowledgeHub, SitePlanner, ConstructionDashboard) to pull inputs/results/setInputs/setResults from usePermitData() instead of props. Leave setActiveTab as a prop for now — that's handled in Part 3.

Part 3 — Real routing
Install react-router-dom. In main.jsx, wrap the app in <BrowserRouter>.

Important — do not rewrite the 46 existing setActiveTab(...) call sites across the app. Instead, keep setActiveTab as the stable API every component already uses, implemented on top of real routing so it's a drop-in replacement:

In App.jsx, derive the active tab from the URL and make setActiveTab navigate:

import { useNavigate, useLocation, Routes, Route } from 'react-router-dom';

// inside App():
const navigate = useNavigate();
const location = useLocation();
const activeTab = location.pathname === '/' ? 'overview' : location.pathname.slice(1);
const setActiveTab = (tab) => navigate(tab === 'overview' ? '/' : `/${tab}`);
Replace the renderTab() switch with actual <Routes>/<Route> elements — one route per existing case (/, /siteplanner, /intake, /permits, /milestones, /docs, /simulation, /compliance, /copilot, /executive, /knowledge, /construction), each rendering the same component that case currently renders. Every child component keeps calling setActiveTab('someTab') exactly as it does today — that now just performs a real navigation, so none of those 46 call sites need to change. The sidebar's renderSidebarNavItem keeps working unmodified too, since it also just calls setActiveTab.

Verify: the URL bar actually changes when navigating between tabs (e.g. /intake, /permits), browser back/forward buttons work correctly between tabs, and a hard refresh on /permits doesn't 404 or dump you back to Overview (client-side routing must handle refresh — if this is server-rendered from vercel.json's rewrite rule already redirecting everything to /index.html, confirm that still applies here; check permitOS/vercel.json equivalent / the root vercel.json rewrite rule already present).

Part 4 — Milestone Timeline: dynamic filter + empty state
In permitOS/src/components/MilestoneTimeline.jsx:

CATEGORIES = ['All', 'Air', 'Water', 'Other', 'Brick PermitOS'] (line 105) is hardcoded — always shows all 5 filter buttons regardless of what the site actually needs. Derive it from the same permit-type logic used elsewhere (pull inputs.hasOnSiteGeneration, inputs.hasWaterUse, etc. via usePermitData() per Part 2) so "Air" only appears if hasOnSiteGeneration !== false, "Water" only if hasWaterUse !== false, and "Other"/"Brick PermitOS"/"All" always show.
Right now this page renders a Gantt chart using results?.totalMW || 200 and results?.pathway || {} even when results is null — meaning it silently shows a fabricated placeholder timeline before any screening has actually run. Add an empty state matching the pattern already used in Permits.jsx: if !results, show "No timeline yet — run Site Intake to generate one" with a button routing to /intake (via setActiveTab('intake')), instead of rendering fake data.
Verify: visit Milestone Timeline before running any screening — should show the empty state, not a fake Gantt chart. Run screening on a site with water use turned off — the "Water" filter button should not appear. Confirm the Gantt bars still reflect the real computed pathway/MW after screening (this part isn't broken today, just confirm it still works after your changes).