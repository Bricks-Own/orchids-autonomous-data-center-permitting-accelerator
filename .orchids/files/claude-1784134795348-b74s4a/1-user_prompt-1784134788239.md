I need two fixes applied to the Site Intake / Review flow. Both are precisely diagnosed below — please implement exactly as specified, and verify the actual live behavior after, not just that the build succeeds.

FIX 1 — Traditional vs. Brick-Accelerated timeline shows identical months (12 vs 12)

I traced this by running the real calculation for the default demo site (BigWatt, Tennessee, 8x25MW gas turbines, 20% Brick load reduction):

baseline NOx: 76.6 tpy -> controlled NOx: 61.3 tpy (Brick's 20% reduction)
pathway: { requiresPSD: true, syntheticMinorViable: false, controlledBelowMajor: false }

Brick's 20% emissions reduction isn't enough to bring this site below the synthetic-minor threshold, so syntheticMinorViable is false for the real/Brick-controlled case too, not just for "traditional." In permitOS/src/utils/timelineCalc.js, computeTimelineComparison() only forces syntheticMinorViable: false for the traditional case; it doesn't otherwise differentiate the two scenarios. Both land in the identical "PSD Major" bucket (pathwayMul = 1.0), so computePathwayDuration() produces byte-identical output for both — hence 12 vs 12 months. This is the exact default demo site, not a rare edge case.

Fix: Brick's controls should shave review time proportionally to the actual emissions reduction achieved, even within the same pathway bucket (lower emissions genuinely means simpler BACT analysis and fewer offset requirements — this shouldn't be all-or-nothing).

In permitOS/src/utils/timelineCalc.js:

1a. Add an optional emissionsReductionPct parameter to computePathwayDuration(), defaulting to 0 (so every other caller, including MilestoneTimeline.jsx's separate getGanttTracks(), is unaffected — do not touch MilestoneTimeline.jsx, it has its own duplicated implementation and doesn't need this):

export function computePathwayDuration({ totalMW, isNonAttain, requiresPSD, syntheticMinorViable, projectScenario, emissionsReductionPct = 0 }) {
  const mwFactor = Math.max(0.7, Math.min(1.5, (totalMW || 200) / 200));
  const attainmentMul = isNonAttain ? 1.5 : 1.0;

  const isTrueMinor = !requiresPSD;
  const isSyntheticMinor = requiresPSD && syntheticMinorViable;

  let pathwayMul;
  if (isTrueMinor) pathwayMul = 0.4;
  else if (isSyntheticMinor) pathwayMul = 0.55;
  else pathwayMul = 1.0;

  // Even within the same pathway bucket, demonstrably lower controlled emissions
  // simplify BACT analysis and reduce offset needs — up to 10% faster review,
  // scaled by the actual emissions reduction (capped at 40% reduction credit).
  const reductionMul = 1 - Math.min(Math.max(emissionsReductionPct, 0), 0.4) * 0.25;
  pathwayMul *= reductionMul;

  const airReviewMul = Math.min(1.8, mwFactor * attainmentMul * pathwayMul);
  // ...rest of function body stays identical

1b. In computeTimelineComparison(inputs, results), compute the real reduction percentage from results.baseline.nox vs results.controlled.nox, and pass 0 for traditional (no Brick controls at all) vs. the real value for brickAccel:

export function computeTimelineComparison(inputs, results) {
  const totalMW = results?.totalMW || (inputs?.turbines || 0) * (inputs?.mwPerTurbine || 0) || 200;
  const isNonAttain = inputs?.nonAttainment || false;
  const projectScenario = inputs?.projectScenario || 'greenfield';

  const baselineNox = results?.baseline?.nox || 0;
  const controlledNox = results?.controlled?.nox || 0;
  const emissionsReductionPct = baselineNox > 0 ? Math.max(0, (baselineNox - controlledNox) / baselineNox) : 0;

  const actualPathway = results?.pathway || {};
  const brickAccel = computePathwayDuration({
    totalMW, isNonAttain,
    requiresPSD: actualPathway.requiresPSD,
    syntheticMinorViable: actualPathway.syntheticMinorViable,
    projectScenario,
    emissionsReductionPct,
  });

  const traditional = computePathwayDuration({
    totalMW, isNonAttain,
    requiresPSD: actualPathway.requiresPSD || false,
    syntheticMinorViable: false,
    projectScenario,
    emissionsReductionPct: 0,
  });

  const monthsSaved = traditional.totalMonths - brickAccel.totalMonths;
  const pctFaster = traditional.totalMonths > 0
    ? Math.round((monthsSaved / traditional.totalMonths) * 100)
    : 0;

  return { traditional, brickAccel, monthsSaved, pctFaster };
}

Verify: on the actual default demo site, Traditional vs. Brick-Accelerated must show a real, non-identical month count. Also re-check a true-minor site (small project, well under PSD thresholds) still shows 0 months saved there — Brick genuinely has nothing to accelerate when PSD was never a concern, that's correct, not a bug.

FIX 2 — Revert section naming in Site Intake back to equipment-first nomenclature

In permitOS/src/components/SiteIntake.jsx, section headers were relabeled around permit type instead of equipment during the redesign. Revert to the exact labels used before (same fields, just the SectionHeading label= text):

- "Air Permit Parameters" -> "Generation Equipment"
- "Water Permit Parameters" -> "Data Center & Water Systems"
- "Power / Interconnection Parameters" -> "Power Permitting Parameters"
- "Building Permitting Parameters" stays as-is (already matches).

Do not change which fields live in which section, and do not change the Step 1 "Permit Types Needed" checkbox mechanism — this is a labeling-only fix. Just rename the label= values passed to SectionHeading for these four sections.

Once you're done, ping me — I'll pull, verify the real numbers diverge on the live default site and the labels are correct, run the backend test suite, build, and push to production.