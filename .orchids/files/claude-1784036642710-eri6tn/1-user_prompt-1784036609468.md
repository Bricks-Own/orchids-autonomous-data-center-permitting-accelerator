STILL BROKEN — State select (and likely every Select in the app) renders completely empty dropdown items. Previous fix (adding text-popover-foreground, min-w-fit) fixed color/width but not the actual bug — the item text itself never renders into the DOM at all.

Verified precisely via computed DOM inspection, not just a screenshot: opening the State select produces a popup with correct position, size, colors, and 49 items (matches US_STATES length) — but every single item's outerHTML is:
  <div data-slot="select-item" class="...">
    <div class="flex flex-1 shrink-0 gap-2 whitespace-nowrap"></div>
  </div>
That inner div is <SelectPrimitive.ItemText> from src/components/ui/select.jsx, and it is completely empty — zero text content, for every item, despite SiteIntake.jsx correctly passing the label as children:
  <SelectItem key={s.value} value={s.value}>{s.label}</SelectItem>
And SelectItem's own definition correctly passes children through:
  <SelectPrimitive.ItemText className="...">{children}</SelectPrimitive.ItemText>
So the children ARE being passed correctly at every level of our code — the text is being lost specifically inside @base-ui/react/select's <Select.ItemText> primitive itself.

Investigate: check the installed @base-ui/react version's actual Select.ItemText API — this may require content to be provided via a different mechanism than plain children (Base UI's other primitives in this same file already use a render prop pattern instead of children in places, e.g. SelectPrimitive.Icon uses render={<CaretDownIcon />} — ItemText may have a similar requirement in this version that plain children doesn't satisfy). Check the Base UI documentation/source for the exact version in package.json for Select.ItemText's correct usage, and fix src/components/ui/select.jsx's SelectItem accordingly.

Cross-check against the real reference: scaffold a fresh copy via `pnpm dlx shadcn@latest init --preset b1zM2TW91E --template vite --pointer --rtl` in a throwaway directory (as done in an earlier round) and compare its generated src/components/ui/select.jsx byte-for-byte against ours — if it differs at all, ours has drifted from the real reference and that's very likely the bug.

This affects every Select in the app (State, Turbine/Engine Type, Discharge Pathway, Occupancy Type, Fire Suppression, Emergency Power Config, Power Source Type, Genset Fuel Type) — fix the shared component once, don't patch it per-instance.

VERIFY properly this time: open the State select, inspect actual rendered DOM (not just a screenshot) to confirm each item's text node is present and non-empty, then actually click a state and confirm the trigger updates to show the selected state's label and inputs.state changes. Do the same for at least 2 other Selects in the app. Zero console errors. Build succeeds. 41/41 backend tests pass.