Great work on the Critical/High fixes. Let's do Medium priority next, one at a time, same approach — verify each before moving to the next.

1. Milestone Gantt chart static — src/components/MilestoneTimeline.jsx:29-54
GANTT_TRACKS has hardcoded start/end weeks for every bar regardless of site data. Make bar durations scale with real factors: total MW, attainment status (STATES_ATTAINMENT), and permit pathway (results.pathway — PSD major vs synthetic minor should show meaningfully different air review durations).

2. Regulator Copilot placeholder text — RegulatorCopilot.jsx lines 23, 37, 44
Templates contain unfilled [X], [Y µg/m³], [Z µg/m³] shown directly to users. Replace with the actual computed values already available in results/inputs (e.g. results.controlled.nox for the NOx value), same pattern other templates in this file already use.

3. Knowledge Hub keyword-only search — server/web-knowledge.js:199-236
Currently simple term-count matching, no semantic understanding. Either relabel the UI to accurately describe it as keyword search (not "AI-powered"), or improve ranking with better matching (synonyms, phrase weighting) — flag if true semantic search would require the AI key to do properly.

4. Tennessee wrong grid operator — src/utils/buildPowerCalc.js:31, also fix the test at server/tests/run.js:347
Tennessee is listed under PJM; it should be TVA (non-ISO, vertically integrated utility). Fix the mapping and update the test assertion to match.

5. Cooling Water Use hint contradicts formula — SiteIntake.jsx:351 vs calculations.js:88-90
The hint says "makeup + blowdown" but the formula treats coolingMGD as a separate circulation rate and adds blowdown on top, which double-counts it. Decide the correct physical model and make the label and formula agree — recommend: coolingMGD = evaporation rate only, formula stays as-is, just fix the label text.

6. Site Intake fires API on every keystroke — SiteIntake.jsx:113-122
useEffect depends on [scenario, inputs], and inputs changes on every keystroke, triggering analyzeScenario() constantly. Add a debounce (~500ms after the user stops typing) before firing the request.

7. "Submit to Agency" doesn't send externally — server/routes.js:496-511
Generates a fake tracking ID and saves locally only, no external request is made. Change all UI wording from "submitted to agency" to something accurate like "saved as draft, ready to file" — don't imply a real filing happened unless we're building real integration later.

Go through in order, confirm each with actual before/after behavior.