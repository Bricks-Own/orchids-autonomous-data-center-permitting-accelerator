MAJOR DESIGN + FLOW REVAMP — Site Intake becomes a proper connected wizard with a new design system, ending in a real review screen. This replaces the visual system from the previous UI pass; the underlying data/calculation logic must NOT be touched except where explicitly specified below.

═══════════════════════════════════════════════════════════════
PART 1 — NEW DESIGN SYSTEM (apply throughout Site Intake + the permit modal)
═══════════════════════════════════════════════════════════════

Full spec below. Read it fully before writing any code — every color/spacing/component value is exact, not approximate.

FONTS
- Primary: Inter (400,500,600,700,800). Monospace (numeric/coordinate fields only — lat/lon, emission factors, MW values): IBM Plex Mono (400,500).
- Add to index.html: <link rel="preconnect" href="https://fonts.googleapis.com"><link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&family=IBM+Plex+Mono:wght@400;500&display=swap" rel="stylesheet">

COLOR PALETTE (zinc-based dark, flat — no gradients, no glow/colored box-shadows, no neon)
- Page bg: #09090b · Card/panel bg: #111113 · Nested surface (badges/chips/inputs/tiles): #18181b
- Border: #27272a · Subtle border: #3f3f46
- Text primary: #fafafa · secondary: #a1a1aa · tertiary/hint: #71717a · mid-tone: #d4d4d8 / #e4e4e7
- Primary action bg: #fafafa (white), text on it: #09090b
- Focus ring: border-color #52525b + box-shadow 0 0 0 3px #ffffff14
- Semantic (status meaning ONLY, never decoration): success #4ade80, warning #e0a95c, destructive #f87171
- BRAND BLUE — very limited, like YouTube's red: YouTube's entire UI is grayscale except a handful of specific brand touchpoints (logo, subscribe button, a few active-state accents) — everything else stays neutral. Use a blue (pick a clean mid-tone blue, e.g. #3b82f6 family) ONLY for: the Brick PermitOS logo/wordmark, and ONE other small deliberate touch such as the active Stepper step or a "Brick-accelerated" badge/highlight that specifically calls out Brick's own value-add (see Part 4). Do NOT use blue for regular buttons, regular badges, form focus states, or general UI chrome — those stay strictly per the white/zinc system above. If in doubt, leave it zinc, not blue.
- No emojis anywhere. Icons only, from @phosphor-icons/react (already installed), duotone weight, used sparingly (one icon per section/card/button max, not decorative icon soup).

TYPOGRAPHY SCALE: page title 22px/700, section title 15px/700, body 13px/400, field label 11.5px/500, field value/input 13.5px/400, hint/caption 11px/400, stepper label 12.5px/500-700, badge/pill text 11-12px/600-700. Letter-spacing -0.02em on large headings only.

LAYOUT: max container width 1180px fluid (never fixed, never horizontal-scroll on narrow viewports). Page padding 36px 40px 48px. Card padding 28px 32px. Card radius 16px. Inner tile/badge radius 8-10px. Pill radius 20px. Field grid 2-column (1fr 1fr), gap 20px 28px. Field label margin-bottom 7px. All spacing via flex/grid gap, not manual sibling margins.

COMPONENTS (build/refactor as shared components, reused everywhere in this pass):
- Card: bg #111113, border 1px #27272a, radius 16px, padding 28px 32px.
- Input/Select: bg #09090b, border 1px #27272a, radius 8px, padding 11px 13px, color #fafafa, 13.5px. Focus: border #52525b + ring per above.
- Primary button (main CTA — "Generate", final submit): bg #fafafa, color #09090b, no border, radius 8px, padding 11px 22px, weight 600, 14px. Hover: brightness(1.15).
- Secondary button ("Continue", "Change", step nav): bg #18181b, border 1px #27272a, color #fafafa (or #d4d4d8 lower emphasis), radius 8px, padding 11px 22px, weight 600, 13.5px.
- Ghost/disabled button ("Back" etc.): bg transparent, border 1px #27272a, color #3a4558 disabled / #d4d4d8 enabled, radius 10px, padding 12px 20px.
- Status badge/pill: bg #18181b, color #a1a1aa (or semantic color for emphasis), 11-12px/600-700, padding 6px 14px, radius 20px.
- Stepper (new — build this, doesn't exist yet): circles 30x30px round. Active: bg #fafafa, color #09090b, no border. Completed: bg #27272a, color #e4e4e7, border 1px #3f3f46, checkmark icon instead of number. Upcoming: bg #18181b, color #71717a, border 1px #27272a. Connector line 1px tall, bg #3f3f46 (completed) or #27272a (upcoming), margin 0 16px. Label 12.5px, weight 700 active / 500 otherwise, color #fafafa active / #d4d4d8 completed / #71717a upcoming. Wrap in a card: bg #111113, border 1px #27272a, radius 14px, padding 18px 26px.
- Slider (Brick Load Reduction %, already exists — restyle): track height 5px radius 3px bg #27272a, fill bg #fafafa flat, thumb 14x14px circle bg #fafafa with ring box-shadow 0 0 0 3px <card-bg>.
- Timeline/milestone list (for the Review step, Part 4): vertical connector 2px wide bg #27272a absolutely positioned; dot 12x12px circle border 2px <card-bg>, fill = semantic color per status or #71717a not-yet-started.

═══════════════════════════════════════════════════════════════
PART 2 — SITE INTAKE BECOMES A 3-STEP WIZARD (using the new Stepper component)
═══════════════════════════════════════════════════════════════

Replace the current single-scroll-page-plus-modal structure with an actual wizard, Stepper at the top:

STEP 1 — "Project Setup" (replaces the current PermitSelectionModal — keep it as a modal OR inline as step 1 content, your call, but it must now capture TWO decisions, not one):
  (a) Permit types needed (Air/Water/Building/Power) — exactly as already built, keep the card-grid multi-select UI, just reskin to the new design system.
  (b) Project Type — Greenfield / Expansion / Upsized / Co-located. THIS IS CURRENTLY MISSING AS A REAL FIELD. It exists today only as local component state inside the "Scenario Analyzer" card at the bottom of the page (src/components/SiteIntake.jsx, `const [scenario, setScenario] = useState('greenfield')` around line 79) — it is NEVER saved to `inputs` and has ZERO effect on anything else in the app (confirmed: src/components/MilestoneTimeline.jsx has no reference to scenario/project-type at all). Fix this: add a real inputs.projectScenario field (values: 'greenfield' | 'expansion' | 'upsized' | 'colocated'), selected via a card-grid identical in style to the permit-type grid, using the existing definitions already in server/scenarios.js SCENARIO_DEFS (label, description, icon→replace emoji with a Phosphor icon, complexity, typicalTimelineMonths) as the source of truth — don't invent new copy, reuse what's there.
  Both selections save to `inputs` and gate Step 2 (must pick at least 1 permit type and exactly 1 project type to proceed).

STEP 2 — "Site Details" (the existing dynamic form): same field-visibility logic as today (only show Air/Water/Building/Power sections for selected permit types), fully restyled to the new design system (Card/Input/Select components above). See Part 3 for a field-completeness pass.

STEP 3 — "Review" (NEW — see Part 4 in full).

Stepper nav: Back/Continue (secondary/ghost button styles) at the bottom of each step. The existing "Generate My Permits" action becomes the final action on Step 3, not Step 2.

═══════════════════════════════════════════════════════════════
PART 3 — FIELD COMPLETENESS AUDIT (Step 2)
═══════════════════════════════════════════════════════════════

Before restyling Step 2, cross-reference every field actually consumed by server/scenarios.js's milestone generator (already reuses inputs.state, totals) — but more importantly by src/utils/calculations.js, src/utils/documentGenerator.js, and src/components/MilestoneTimeline.jsx's getGanttTracks() — against what's currently collected per permit-type section. Confirm nothing needed for document generation or the Gantt chart is missing from the form for whichever permit types are selected. (Most fields were already audited and fixed in a prior pass — turbine/genset split, genset fuel type, discharge pathway — this is a final completeness check, not expected to find much, but verify.)

═══════════════════════════════════════════════════════════════
PART 4 — STEP 3 REVIEW SCREEN (the most important part — must be excellent)
═══════════════════════════════════════════════════════════════

Requirements: crystal clear, metrics-first, NOT walls of text. Someone should be able to scan this in 15 seconds and understand their permitting picture. Use Cards, badges, and the timeline component from Part 1 — minimal prose, real numbers front and center.

Sections, top to bottom:
1. Site summary strip: site name, state/county, project type badge, permit types as badges. One line, not a paragraph.
2. THE HEADLINE METRIC — Traditional vs. Brick-Accelerated Timeline. This is the single most important thing on this screen and it currently DOES NOT EXIST as a real computed comparison anywhere (the closest thing is a static hardcoded line "Phase 1 → instant (vs. 2-3 weeks manual)" in the current intro card, which is not calculated from anything and must be replaced). Build this for real, using data that's already computed — do not invent a number:
   - "Traditional" pathway = permitting timeline assuming the project's BASELINE (uncontrolled) emissions profile. calculations.js already computes `requiresPSD` from baseline totals (calculations.js ~line 87: `criteriaPollutants.some(p => totalBaseline[p] >= PSD_THRESHOLD)`) — if true, this project would require full PSD review without any Brick optimization.
   - "Brick-Accelerated" pathway = the timeline using the project's CONTROLLED (Brick-optimized) emissions profile. calculations.js already computes `isSyntheticMinorViable` from controlled totals (~line 89: `criteriaPollutants.every(p => controlled[p] < PSD_THRESHOLD)`) — if Brick's dispatch controls (driven by the brickSavings input) keep the project's controlled emissions under major-source thresholds, it qualifies for the much faster synthetic-minor pathway instead of full PSD.
   - MilestoneTimeline.jsx's getGanttTracks() (~line 14-32) already encodes the real duration difference between these pathways via `pathwayMul` (0.4 for true minor / 0.55 for synthetic minor / 1.0 for full PSD major) applied to the air review/issuance week ranges. Reuse this exact logic (refactor into a shared function both MilestoneTimeline and this review screen call, e.g. in a new src/utils/timelineCalc.js) to compute two real end-to-end week/month totals: one running the pathway math with requiresPSD=true/syntheticMinor=false (traditional, no Brick optimization applied) and one with the project's actual computed pathway (Brick-accelerated, using controlled emissions). Also apply the project-type-specific scaling already defined in server/scenarios.js SCENARIO_DEFS.typicalTimelineMonths so a Co-located project and a Greenfield project don't show the same baseline number.
   - Display as: two large numbers side by side or stacked — "Traditional: ~X months" (muted/gray) vs "Brick-Accelerated: ~Y months" (the one blue-accent touch allowed per Part 1, OR use the success-green semantic color since it represents a positive outcome — your call, pick one and be consistent) — plus a "Z% faster" or "N months saved" badge. This must recompute live from the actual site data (different site data → different numbers) and must be IDENTICAL in methodology to whatever MilestoneTimeline.jsx shows elsewhere — these two numbers must never contradict each other since they come from the same shared function.
3. Permit-by-permit breakdown: for each SELECTED permit type only, a compact card showing 2-4 key metrics (e.g. Air: baseline vs controlled NOx/CO tpy + pathway determination; Water: discharge pathway + NPDES applicability; Building: IBC class + occupancy; Power: interconnection voltage + FERC status) — pull these from the already-computed `results` object, do not recompute.
4. Compact milestone/timeline preview: use the Part 1 timeline component to show 4-6 major phase markers (not the full detailed Gantt from MilestoneTimeline.jsx — a simplified summary version) with a "View full timeline →" link to the real Milestone Timeline tab.
5. Final action: the "Generate My Permits" primary button lives here now, at the bottom of Step 3, not Step 2.

═══════════════════════════════════════════════════════════════
CONSTRAINTS
═══════════════════════════════════════════════════════════════
- Do not modify calcPTE's actual emissions math, PSD_THRESHOLD logic, or any existing field's data key. You MAY add inputs.projectScenario as a new field and refactor getGanttTracks()'s pathway-multiplier logic into a shared/reusable function as specified in Part 4 — that's it for calculation-layer changes.
- Do not modify the dynamic import paths in server/routes.js ('./lib/...') — every past PR has reverted these, please leave them alone this time.
- This pass is scoped to Site Intake + the review screen only. Other tabs (Air/Water/Building/Power results pages, Construction Platform, etc.) keep their current styling for now — more design-system rollout will follow in later prompts, don't touch them yet.
- Keep the existing permit-type filtering logic in Header.jsx (nav pruning) working exactly as today.

VERIFY: fresh site → Step 1 requires both a permit-type and project-type selection before Continue enables → Step 2 shows only relevant sections, new design system applied, no overflow at 1280px or ~768px width → Step 3 Review shows the traditional-vs-Brick headline metric with two different real numbers when you change brickSavings or turbine count (prove it's not static) → change Project Type from Greenfield to Co-located and confirm the review's timeline numbers change accordingly and MATCH what the real Milestone Timeline tab shows for the same inputs → Generate button on Step 3 still runs screening correctly → zero console errors → 41/41 backend tests pass → build succeeds. No emojis anywhere, blue used only where specified, everything else strictly zinc/white per the palette.