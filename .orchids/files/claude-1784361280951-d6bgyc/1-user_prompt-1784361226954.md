Two things to do. Part 1 is a sync note about direct edits already made outside of Orchids (do not revert these — they are already live in production). Part 2 is a new fix: make the Water Permit determination real instead of a checkbox echo.

═══════════════════════════════════════════
PART 1 — SYNC: two direct production edits that already exist. Confirm these stay exactly as-is, do not regenerate or revert them.
═══════════════════════════════════════════

1. src/utils/timelineCalc.js — the timeline reduction is now a FLAT 40%, not a 30-40% range. The constant is:
   const TIMELINE_REDUCTION_TARGET = 0.40;
   Used in computeTimelineComparison(): when a site requires PSD and has a real emissions reduction, pctFaster = Math.round(TIMELINE_REDUCTION_TARGET * 100) = 40, always — never scaled by emissionsReductionPct. True Minor sites (no PSD) still show 0%. Do not reintroduce TIMELINE_REDUCTION_MIN/MAX or any scaling logic — 40% must be flat and identical on the Overview page, Site Intake Step 3, and Milestone Timeline.

2. src/components/ConstructionDashboard.jsx — the "Lien Waiver Compliance" KPI card (in the Financial/Cost section) now shows gray/N-A on a fresh site instead of a hard red "N — CRITICAL". Current correct code:
   <KpiCard title="Lien Waiver Compliance" value={data.lienWaiverCompliance || 'N'} status={(data.billingToDate === 0 && evm?.percentComplete === 0) ? 'gray' : data.lienWaiverCompliance === 'Y' ? 'green' : 'red'} subtitle={`${data.lienWaiversReceived || 0} received`} />
   Keep this noData-aware pattern — it matches the same pattern already used for statusCPI/statusSPI/statusInspection elsewhere in this file and in server/constructionData.js's getTrafficLight().

═══════════════════════════════════════════
PART 2 — NEW FIX: real, threshold-based Water Permit determination (replacing the current yes/no echo)
═══════════════════════════════════════════

CURRENT BROKEN BEHAVIOR:
In src/components/SiteIntake.jsx line 100, whether the Water Permit card shows at all is:
  inputs.hasWaterUse !== false && 'water'
This is a direct, unevaluated echo of the Step 1 Yes/No click. It does not look at real water volume, discharge pathway, or any regulatory threshold. A site with trivial, permit-exempt water use still shows "requires Water Permit" just because the user clicked Yes in Step 1. This same derivedPermitTypes pattern is duplicated in src/components/Permits.jsx, src/components/DocumentFactory.jsx, src/components/MilestoneTimeline.jsx, and src/components/SitePlanner.jsx.

WHAT TO BUILD INSTEAD — real threshold logic, no AI, just rule-based comparisons against actual regulatory numbers:

1. In src/utils/calculations.js, inside the water calc block (around line 100-124, right after `water: { annualWaterMG, blowdownMG, makeupMG, optimizedWater }`), add a new `determination` object computed from real inputs:

   - dischargePathway = inputs.dischargePathway (existing field, values: 'Surface Water Discharge', 'POTW-Sanitary Sewer Connection', or '' /unset)
   - aggregateDieselGal = existing estimateDieselStorage logic currently duplicated in WaterPermitAI.jsx (count * hp * 1.2) — move this helper function into calculations.js so it's the single source of truth, and export it.
   - spccRequired = aggregateDieselGal > 1320  (SPCC Rule, 40 CFR Part 112 — federal threshold, do not make this configurable)
   - requiresNPDES:
       - false if dischargePathway is '' (unset) or if annualWaterMG === 0 and no process/blowdown discharge — i.e. genuinely no discharge to evaluate yet
       - false if dischargePathway === 'POTW-Sanitary Sewer Connection' (goes to sewer, not VPDES — pretreatment permit applies instead, not NPDES)
       - true if dischargePathway === 'Surface Water Discharge' AND (blowdownMG / 365) > 0 (i.e. there's an actual ongoing discharge flow, not just a hypothetical selection)
   - permitTier (only meaningful when requiresNPDES is true):
       - dischargeMGD = blowdownMG / 365
       - 'Major (Individual Permit + EPA Review)' if dischargeMGD >= 1.0   // Virginia VPDES major-source threshold
       - 'Minor (General Permit)' if dischargeMGD < 1.0 and > 0
   - pretreatmentRequired = dischargePathway === 'POTW-Sanitary Sewer Connection'
   - requiresAnyWaterPermit = requiresNPDES || pretreatmentRequired || spccRequired
     (this is the real gate — a site can need an SPCC plan for its generator diesel even if it has zero water discharge, so "any water permit" is not just about NPDES)

   Return this as `water: { annualWaterMG, blowdownMG, makeupMG, optimizedWater, determination: { requiresNPDES, permitTier, pretreatmentRequired, spccRequired, requiresAnyWaterPermit, dischargeMGD } }`

2. In src/components/SiteIntake.jsx line 100, replace:
     inputs.hasWaterUse !== false && 'water'
   with:
     results?.water?.determination?.requiresAnyWaterPermit && 'water'
   Fall back to the old hasWaterUse check ONLY if results is not yet computed (i.e. before the user has run screening) so the input-collection Card at line 444 (the "Data Center & Water Systems" fields — Cooling Water MGD, Blowdown %, Process Water MGD, Discharge Pathway) still appears pre-screening based on the Step 1 answer, since you need those fields filled in before determination is possible. So the logic should be:
     (results?.water?.determination ? results.water.determination.requiresAnyWaterPermit : inputs.hasWaterUse !== false) && 'water'

3. Apply the same corrected derivedPermitTypes logic to src/components/Permits.jsx, src/components/DocumentFactory.jsx, and src/components/MilestoneTimeline.jsx (find their local copies of the water permitTypes check and replace identically) — and src/components/SitePlanner.jsx's "Permit Requirements" card should show the same real result once results exist, otherwise fall back to the Step 1 answer same as above.

4. In src/components/SiteIntake.jsx Step 3 Review card (lines 710-739, the Water Permit card), replace the current badge logic:
     <Badge variant="secondary" className={inputs.dischargePathway === 'Surface Water Discharge' ? 'text-[#e0a95c]' : 'text-primary'}>
       {inputs.dischargePathway === 'Surface Water Discharge' ? 'Individual Permit Likely' : 'General Permit / POTW'}
     </Badge>
   with real determination-driven text:
     {results.water.determination.pretreatmentRequired
       ? 'Pretreatment Permit (POTW)'
       : results.water.determination.requiresNPDES
         ? `VPDES ${results.water.determination.permitTier}`
         : 'No NPDES Permit Required'}
   and add a second badge showing SPCC status: 'SPCC Plan Required' (amber) if results.water.determination.spccRequired else 'SPCC Not Triggered' (muted/secondary) — mirroring the badge pattern already used in src/components/WaterPermitAI.jsx line 53-55.

5. In src/components/WaterPermitAI.jsx, remove the duplicated local cgpRequired/spccRequired/potwPathway calculation (lines ~34-42) and instead read spccRequired and pretreatmentRequired directly from results.water.determination, so there is exactly one place (calculations.js) computing these values — no duplicate logic that can drift out of sync.

Do not touch AirPermitAI.jsx, the calcPTE air pathway logic, or any Building/Power permit logic — this fix is scoped to water only.