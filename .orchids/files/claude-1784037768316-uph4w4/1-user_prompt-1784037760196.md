GROUND-TRUTH REFERENCE PROJECT + SIDEBAR/HEADER STRUCTURE + GLOBAL TYPOGRAPHY FIX

═══════════════════════════════════════════════════════════════
STEP 1 — Scaffold a real reference project, use it as ground truth, do NOT commit it.
═══════════════════════════════════════════════════════════════
In a scratch/temporary directory OUTSIDE this project's repo (e.g. /tmp or a sibling folder — NOT inside permitOS, and this scaffolded project must never be committed or pushed to this repo's git history), run:
  yarn dlx shadcn@latest init --preset b1zM2TW91E --template vite --monorepo --rtl --pointer
This generates a full working reference app (not just bare component files) using this exact preset. Open it and actually run it locally. Use its real, working source code — page composition, not just isolated ui/ primitive files — as the literal ground truth for how Sidebar, Select, Slider, headings, type scale, and icon sizing are meant to look and be composed together in a real page. Specifically:
- Compare its Select usage against ours (src/components/ui/select.jsx, and how SiteIntake.jsx uses SelectItem) — confirm our current implementation matches how the reference actually renders item text; we've hit two separate item-rendering bugs in this component already.
- Compare its Sidebar composition (header/logo area, collapse trigger placement, nav item behavior on collapse) against ours in src/App.jsx — use its actual patterns for the fixes in Step 2 rather than reinventing them.
- Use its actual computed heading sizes, body text sizes, and icon sizes as ground truth for Step 3 rather than picking new arbitrary numbers.
- Port over any concrete corrections found this way into our real src/components/ui/*.jsx, src/App.jsx, and src/components/SiteIntake.jsx files. Delete/discard the scratch reference project when done — it is a local research tool only, never part of this repo.

═══════════════════════════════════════════════════════════════
STEP 2 — Sidebar/Header structural fixes (use the reference project's real pattern for these)
═══════════════════════════════════════════════════════════════
(a) Header/sidebar-header height mismatch: the top Header bar (src/components/Header.jsx) and the Sidebar's own header/logo section (src/App.jsx, SidebarHeader) currently have different heights, so their bottom border lines don't align horizontally across the page. Make both sections the exact same height so the border/seam lines up as one continuous line across the full width of the page.

(b) Remove duplicate branding. The top Header bar currently repeats "Brick PermitOS — Data Center Permitting Intelligence" even though the Sidebar's own header already shows the logo + "Brick PermitOS" name. Remove this text from Header.jsx entirely — it's redundant. The top bar should just contain the status badges/logout that are already there, no duplicate branding.

(c) Nav labels must not wrap. Sidebar menu item labels (Platform Overview, Executive Summary, etc.) must use whitespace-nowrap + text overflow truncation, not wrapping onto a second line, so that collapsing the sidebar to icon-only is a clean width/opacity transition with no layout jerk or reflow as the label disappears.

(d) Move the collapse/expand trigger into the Sidebar's own footer, above "Sign Out" — not in the top Header bar where it currently is. It's a sidebar-owned control, not a page-header control; placing it in the SidebarFooter (in src/App.jsx, above the existing Sign Out SidebarMenuItem) is more logical. Remove the SidebarTrigger from Header.jsx once relocated.

(e) Fix the disappearing bottom spacing on the sidebar logo section when collapsed. When the sidebar collapses to icon-only, the logo/header area currently loses its bottom padding/margin entirely (traced to the group-data-[collapsible=icon]:p-0 / py-0 override added in an earlier fix, which zeroed ALL padding including spacing that should be preserved before the separator line below it). The collapsed logo section still needs appropriate bottom spacing so it doesn't sit flush against the separator — adjust so only the horizontal padding collapses (to center the icon) while vertical spacing before the separator is preserved.

═══════════════════════════════════════════════════════════════
STEP 3 — Global typography scale fix (sweep the ENTIRE app, not just Site Intake)
═══════════════════════════════════════════════════════════════
Current type scale is inconsistent and mostly too small — the result of many rounds of arbitrary pixel classes (text-[11px], text-[10px], text-[13px], etc.) applied ad hoc instead of a real, consistent scale.

RULE:
- 12px (Tailwind's text-xs) is the SMALLEST font size allowed anywhere in this app — captions, hints, tiny labels only. Nothing renders smaller than this.
- 14px (Tailwind's text-sm) is the DEFAULT body/general text size — form field values, descriptions, list items, badges, buttons — this should be the single most common text size across the app.
- Body font stays Outfit (the preset's --font-sans), applied via the existing default — don't override per-component.

Sweep the entire src/components/ directory (not just files touched by prior passes) for any remaining arbitrary text-[Npx] class or any text rendering smaller than 12px, and remap every one to the correct semantic Tailwind class (text-xs = 12px floor, text-sm = 14px default) — no more one-off pixel values anywhere.

HEADINGS: increase heading sizes (Raleway, the --font-heading tokens) across the app — page titles, card section titles, and any h1/h2/h3-equivalent text are currently too small relative to a real product UI. Use the Step 1 reference project's actual h1/h2/card-title computed font-size values as ground truth rather than picking new arbitrary numbers.

ICONS: increase icon sizes app-wide to match the heading/type scale fix — again, use the reference project's actual icon size values (nav icons, card header icons, button icons) as ground truth rather than guessing. Icons should feel proportionate to the larger text, not left behind at the old small sizing.

═══════════════════════════════════════════════════════════════
CONSTRAINTS
═══════════════════════════════════════════════════════════════
Presentation/structure only — no data logic, calculation, or field-wiring changes. Don't touch server/routes.js's './lib/...' import paths. Preserve all existing functionality: permit-type + project-type selection, dynamic form sections, the Traditional-vs-Brick timeline comparison, sidebar nav-pruning logic. The scratch reference project from Step 1 must never appear in this repo's git history — confirm it was created outside the project directory and never staged/committed.

═══════════════════════════════════════════════════════════════
VERIFY
═══════════════════════════════════════════════════════════════
- Screenshot the page showing the Header bar and Sidebar header bottom borders aligned as one continuous line.
- Screenshot the sidebar collapsing and confirm labels disappear cleanly with no wrap/jerk.
- Confirm the collapse trigger is now in the footer above Sign Out and no longer in the top Header bar.
- Confirm the top Header bar no longer shows the duplicate "Brick PermitOS" text.
- Confirm the collapsed sidebar's logo section still has proper spacing above the separator line, not flush/cramped.
- Sample at least 10 different text elements across different pages/components via computed font-size and confirm none render below 12px, and that 14px is genuinely the dominant/default body size.
- Screenshot at least 3 different pages showing corrected heading and icon sizes.
- Re-verify the State select still shows visible, selectable option text after these structural changes (this was just fixed — confirm it survives this pass).
- Zero console errors. Build succeeds. 41/41 backend tests pass.